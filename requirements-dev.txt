# Dev tooling — pinned to versions current in 2023 (project timeline).
# Production/runtime deps (pyspark, great-expectations, etc.) land with their
# phases and follow the same pinning policy.
pre-commit==3.5.0
black==23.11.0
isort==5.13.0
ruff==0.1.9
pytest==7.4.3
